# Constants
# Not an object but references an object, the same way that variables do. 
# Use for stroing values that are constant. 
# Convention is that it's named using ALL uppercase. 

# Should NOT be changed but can be changed, Ruby will give a warning.  
# Although constants are supposed to be written in all uppercase, but it's actually the first letter that makes the difference. If the first letter is capitalized, Ruby considers it to be constant. 

MAX_SCORE = 100

MAX_SCORE = 50
# Ruby warning: already initialized constant

